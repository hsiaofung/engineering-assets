# 新增功能描述:
使用者輸入服務的類別，系統立刻根據匹配程度排序，推薦最符合的長照機構，優先顯示距離近又匹配服務類型的長照機構。


## 新增功能核心流程

- result page增加流程:

    - 使用者進入result 頁面，看到明顯的服務類別輸如欄

    - 使用者一個一個加入服務類別標籤式輸入(tag-style input)：輸入後按 Enter，顯示成膠曩標籤(pills)）

    - 使用者按下篩選長照機構

    - 應用程式回傳依服務類別匹配排序的卡片列表。