而且這可以當成一個很合理的 technical design decision：

When no default landing page is specified by UX, the Compute module displays the tree without automatically navigating to a specific resource page.

也就是：沒有 UX 定義，就不自行決定 default page。