# Purpose

將 Backend Tree API 轉成 Tree Component 所需格式。

# Scope

例如：
```json
Backend API

↓

Tree Adapter

↓

TreeNode[]
```
# Acceptance Criteria
- Backend Response 可轉換成 TreeNode
- UI 不直接依賴 API Schema